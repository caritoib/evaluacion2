{{> navbar}}


<div class="container mt-3">
  <header class="my-5">
    <h1 class="titulo">Contactos</h1>
    <p>
      Guarda de manera segura tus contactos!
    </p>
    <a href="/about">¿Cómo funciona?</a>
  </header>
  <hr>
  <h3 class="mt-5 mb-3">Contactos</h3>
  
    <div class="card mb-3">
      <div class="card-body">
        <table class="table table-border table-hover table-sm">
          <thead>
          <tr>
            <th>Imagen</th>
            <th>Nombre</th>
            <th>telefono</th>
            <th>Email</th>
            <th>Acciones</th>
          </tr>
          </thead>  
          <tbody>
          {{#each persons}}
            <tr>
              {{#if this.picture }}
                <td><img src="{{this.picture}}" width="50px"</td>
              {{else}}
                <td><img src="img/sin-imagen.jpg" width="50px"</td>
              {{/if}}
              <td>{{this.first_name}} {{this.last_name}}</td>
              <td>{{this.phone}}</td>
              <td>{{this.email}}</td>
              <td><a href="/contacts/{{this.id}}" class="btn btn-success"><i class="fas fa-search"></i> DETALLES</a></td>
            </tr>
          {{/each}}            
          </tbody>
        </table>
      </div>
    </div>
</div>